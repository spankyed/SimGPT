
import {
  Scene,
  FreeCamera,
  Vector3,
} from "@babylonjs/core";

export function setPositions(scene: Scene) {
  // task.loadedMeshes.forEach((mesh: any) => {
  //   mesh.position = new Vector3(0, 0, 0);
  // });
}
